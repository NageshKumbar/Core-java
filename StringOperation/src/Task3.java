public class Task3 {
    public void task(String word) {
        int v_count = 0;
        int c_count = 0;
        for (int index = 0; index<word.length() ; index++) {
            if(word.charAt(index) == 'a' || word.charAt(index) == 'e' || word.charAt(index) == 'i' || word.charAt(index) == 'o' || word.charAt(index) == 'u') {
                v_count += 1;
            }else {
                c_count += 1;
            }
        }
    }

    public void
    
}
